<!DOCTYPE html>
<html lang="en">
    <head>
    <%- include('../partials/head.ejs')%>
    <link rel="stylesheet" href="/static/">
    </head>
<body>
    <h1>FRIDAY</h1> 
    <a href="/planner/friday/new"><button>Create new task</button></a>

        <% for(task of tasks) { %>
            <a href="/planner/friday/<%=task._id%>"><h3><%=task.taskName%></h3></a><h5><%=task.date%></h5><p><%=task.urgent ? "urgent" : "" %></p>
        <% } %>

        <a href="/planner"><button>Back to Planner</button></a>
</body>
</html>